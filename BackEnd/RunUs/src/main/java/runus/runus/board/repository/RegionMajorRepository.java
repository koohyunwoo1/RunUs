package runus.runus.board.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import runus.runus.board.entity.RegionMajor;

public interface RegionMajorRepository extends JpaRepository<RegionMajor, Integer> {
}
